import { isFinite } from '@annexe/number.isfinite';
import { isFloat } from '@annexe/number.isfloat';
import { isNumber } from '@annexe/number.isnumber';

const number = {
  isFinite,
  isFloat,
  isNumber
};

export default number;
