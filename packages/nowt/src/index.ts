import { isNil } from '@annexe/nowt.isnil';
import { isNull } from '@annexe/nowt.isnull';
import { isUndefined } from '@annexe/nowt.isundefined';

const nowt = {
  isNil,
  isNull,
  isUndefined
};

export default nowt;
